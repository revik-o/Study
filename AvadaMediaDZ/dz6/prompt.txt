ng serve 

export PATH="$PATH:/home/oleg/jdk-15.0.2/bin/"
./gradlew clean
./gradlew bootRun


keytool -genkey -alias adminREST -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore adminREST.p12 -validity 750
# admin1 //password

sudo sh -c 'echo 1 > /proc/sys/vm/drop_caches'

# Reduce the swap tendency 
vm.swappiness = 10
vm.vfs_cache_pressure = 50
vm.watermark_scale_factor = 200
vm.dirty_ratio = 3

fs.inotify.max_user_watches=524288
